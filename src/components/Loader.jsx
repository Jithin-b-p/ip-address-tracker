const Loader = () => {
  return (
    <div className="w-full flex items-center justify-center">
      <img src="/images/loader.svg" alt="loader" width={24} height={24} />
    </div>
  );
};

export default Loader;
